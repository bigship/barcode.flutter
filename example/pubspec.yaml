name: barcode_flutter_demo
description: Demo project for the Flutter Barcode library.

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  barcode_flutter:
    path: ..
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: